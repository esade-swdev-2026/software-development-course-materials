# Normalise line endings in the repository to LF, whatever the contributor is on.
# Without this, a clone made by Git for Windows rewrites setup-check.sh to CRLF and
# the Ubuntu shell cannot run it: "/usr/bin/env: 'bash\r': No such file or directory".
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
